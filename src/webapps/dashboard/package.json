{
  "name": "dashboard",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "lint": "standard src/**/*.js --verbose && standard src/*.js --verbose",
    "build": "npm run lint && browserify src/index.js -t babelify -o dist/bundle.js && browserify src/clients.js -t babelify -o dist/bundle-clients.js && browserify src/vod.js -t babelify -o dist/bundle-vod.js"
  },
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "babel-preset-es2015": "^6.9.0",
    "babelify": "^7.3.0",
    "browserify": "^13.0.1",
    "es6-promise": "^3.2.1",
    "isomorphic-fetch": "^2.2.1",
    "standard": "^7.1.2"
  },
  "dependencies": {
    "chart.js": "^2.2.1",
    "moment": "^2.14.1",
    "video.js": "^5.10.8"
  }
}
